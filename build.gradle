apply plugin: 'java'
apply plugin: 'eclipse'

repositories {
    mavenCentral()
}

dependencies { 
	testCompile(
		"junit:junit:4.12",
		"org.mockito:mockito-core:2.+"
	)
}

task showMeCache << {
  configurations.compile.each { println it }
}
